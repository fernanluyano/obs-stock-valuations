{
	"name": "stock-valuations",
	"version": "0.1.0",
	"description": "Calculate WACC, DCF, Graham, and Ten Cap intrinsic value and insert the results into your note.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "vitest run"
	},
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.14.2",
		"builtin-modules": "^3.3.0",
		"esbuild": "0.21.5",
		"obsidian": "latest",
		"tslib": "2.6.2",
		"typescript": "5.4.5",
		"vitest": "^2.1.9"
	},
	"dependencies": {
		"chart.js": "^4.5.1"
	}
}
